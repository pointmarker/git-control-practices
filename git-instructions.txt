-the first line
-git log = to see what did you committed
-git restore <filename> - git checkout -- <filename> = it will change your file with the situation of the last commit of that file
-git branch -M <branchname> branchname adındaki branche geçiş yapar, yoksa oluşturur ve öyle geçiş yapar
-git branch <branchname> branchname adında branch oluşturur
-git checkout <branchname> geçiş yapar
-git push -u origin <branchname> branchname için maine bağlı bi branch oluşturur remote tarafında
-merge sırasında commit mesajı değiştirmek istermisin diyinde :wq (write & quit) yazarak çıkış yapılır merge tamamlanır
-git commit --amend, localdeki son commiti değiştirmeye yarar, daha önceden push edildiyse tekrar push edilmesi gerekir  
eğer commite ekleme yapılmak istenirse, önce ekleme yapıp stage e ekle, sonrasında amend diyip tekrar push et
bu son commitin içinde o değişikliğin de olmasını sağlayacak
